{
  "name": "mirthgraph_react",
  "packageManager": "yarn@3.4.1",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "s:dev": "yarn workspace server start:dev",
    "s:build": "yarn workspace server build",
    "f:dev": "yarn workspace frontend dev",
    "f:build": "yarn workspace frontend build"
  }
}
